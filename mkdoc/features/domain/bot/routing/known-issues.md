# Known issues

# Active

- Available routing fields depend on what each connector publishes into the normalized event context.
- Adding new routing predicates still requires code changes in the filter layer; the filter surface is not fully data-driven.

# Resolved

(No resolved issues.)
