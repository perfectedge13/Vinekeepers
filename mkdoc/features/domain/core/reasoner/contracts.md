# Contracts

# APIs

None. Reasoner.think(ReasonerInput) → ReasonerOutput.

# Schemas

ReasonerInput: event, context, state. ReasonerOutput: statePatch, proposedActions, nextState.

# Interfaces

Reasoner: think(ReasonerInput) → ReasonerOutput. ReasonerInput, ReasonerOutput models.
