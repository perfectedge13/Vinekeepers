# Change log

# Entries

## 2026-03-23

- **Plan confidence + approval threshold + material-gap pause (mk sync):** Documented **`PlanConfidence`** known-vs-unknown tallies, **`PlanReadinessCalculator.APPROVAL_CONFIDENCE_THRESHOLD`** (**0.85**) for **`evaluate_planning_approval_gate`**, and **`PlanningPostDraftGovernor`** / pipeline behavior: **one concrete clarifying question per cycle** while material gaps remain. Cross-links **workflow-steps** and **state** dossiers. Specs: **`workflow-registry`**, **`state-registry`**.
- **Readiness fork button clarity:** The **`NEEDS_HUMAN_DECISION`** checkpoint guide now stays explanatory and explicitly points to the **next message's buttons**, while the following **`prompt_for_field`** remains the single authoritative **Continue to approval / Revise plan first** decision. Added regression coverage for the v2 configurable-steps rich-reply path and updated workflow spec wording so decision checkpoints stay button-driven while open-ended clarifications remain text-first.

## 2026-03-22

- **Optional Qdrant planning RAG:** Documented **`prep_planning_repo_grounding`** after **`ensure_repo_workspace`**, **`spread_plan_workspace_signals`** merge of **`planningRag*`** into **`planningRepoEvidenceJson`**, and synthesis use of **`planningRagRetrievalText`**. Cross-links **workflow-steps** and **env** dossiers. Specs: **workflow-registry**, **REQ-LUNA-001** acceptance.
- **Clarification fallback UX (open questions):** Intake clarification no longer leaks raw missing-field tokens like **`open_questions_block.backlog.open_questions`** when the plan is missing the open-questions block. **`PlanningPromptFormatter`** now asks for unresolved unknowns with clarification-safe wording, and **`ClarificationPromptQualityGate`** falls back to plain-language copy instead of internal field paths. Tests: **`PlanningPromptFormatterTest`**, **`IntakeBlockingClarificationTest`**. Specs: **`workflow-registry`**.
- **Mk sync (coordinator Discord UX narrative):** **how-it-works** adds **Coordinator planning: operator-visible progress and copy** (progress posts, humanized labels, deduped review, long-phase visibility); **tests** list extended for OpenAI progress and **synthesize_plan_drafts** humanization; **decisions** records progress/dedupe policy alongside existing readiness/packet humanization entries.
- **Planning OpenAI observability:** Thread **Update:** lines and optional truncated redacted payload logging for planning OpenAI calls (**`OpenAiCallContext`**, **`OpenAiPlanningProgressPoster`**, **`OpenAiChatClient`** env toggles). Docs: **workflow-steps** dossier, **env** dossier, **README** / **`.env.example`**. Specs: **workflow-registry**, **env-registry**.
- **Strict Arrietty clarification contract:** Coordinator copy asks **one** **concrete blocking** question only when a **required** fact is not inferable; **`ClarificationPromptQualityGate`** (including **`isSubstantiveOpenQuestionLine`** / **`isInternalMechanismDetail`**) and **`PlanningQuestionRankingPolicy`** (**`maxQuestions` 1** in pipeline) filter generic meta/completeness/vague open-question prompts; **`CoordinatorClarificationGapEvaluator`** ignores generic **`questionTemplate`** rules. Packet **open questions** use filtered unresolved lines (**`PlanGovernanceDeriver`**, **`PlanningArtifactTexts`**, **`PlanningThreadPacketFormatter`**). **`PlanningUserFacingCopy`**, **`PlanningPromptFormatter`**, and **`PlanningCyclePipeline`** humanize visible failures (cycle error, LLM lines, merge errors). Docs: README, workflow, workflow-steps change-log, cursor-gathering contracts/how-it-works. Tests/specs as in workflow-steps change-log entry.
- **Planning packet artifact UX (Discord):** **`PlanningThreadPacketFormatter`** now renders tracked **assumptions**, **issues**, **risks**, **decisions**, and **open questions** as **Discord-friendly labeled lists** instead of markdown tables, so inline fields like **Priority**, **Status**, **Severity**, **Impact**, and **Likelihood** remain readable in Discord. Optional **requirements / user-story** companion blocks also render as numbered lists when bullet or numbered text has multiple rows. **Section-level truncation** still uses the explicit *(Truncated for display length.)* footnote, and capped inline item text still ends with **…**. **`BuildRolePlanningThreadMessagesAction`** / **`BuildPlanningThreadReviewBodyAction`** stay aligned with the same humanization patterns where they format artifacts for thread copy; **architect** role copy includes the **full decision log** as a labeled list (not only the first line). **`BuildPlanningThreadReviewBodyAction`** uses an explicit cap footnote when review bodies are truncated. **`PlanGovernanceDeriver`** merges artifact and ledger open-question/decision sources for packet lists in a more operator-friendly way while Phase B discovery stays **one question at a time**. **`PlanningPromptFormatter`** also makes repeatable decision prompts self-contained instead of sending users to labels like **Decision 1**. Tests: **`PlanningThreadPacketFormatterTest`**, **`PlanningPromptFormatterTest`**, **`BuildRolePlanningThreadMessagesActionTest`**, **`BuildPlanningThreadReviewBodyActionTest`**, **`PlanGovernanceDeriverTest`**, **`SynthesizePlanDraftsActionTest`**, plus existing planning/packet tests as updated. Specs: **workflow-registry**, **state-registry**, **config-registry** (where assets were touched).
- **Readiness checkpoint UX (Arrietty):** **`planReadinessStatusLabel`** stays the short status line; **`planReadinessCheckpointGuide`** (canonical copy from **`PlanningUserFacingCopy.readinessCheckpointGuideForDiscord`**) is spread with **`run_plan_critique_and_readiness`** / **`hydrate_planning_session`** when **`planReadinessStatus`** is **`NEEDS_HUMAN_DECISION`** and interpolated in the human-review **`post_channel_message`**. Readiness choice **`prompt_for_field`** copy is plainer; decision-log gap prompts use clearer wording (**`PlanningPromptFormatter`**, **`work-profiles.yaml`**). **Packet/critique/depth Discord copy:** **`PlanningThreadPacketFormatter`**, **`PlanCritiqueSupport`**, **`RunPlanCritiqueAndReadinessAction`**, **`BuildRolePlanningThreadMessagesAction`**, **`PlanningPacketDepthEvaluator`**, **`GenericReadinessEvaluator`**, and **`PlanGovernanceDeriver`** (where titles apply) prefer **`PlanningUserFacingCopy`** and profile field titles so thread posts avoid raw artifact paths and internal tokens. Tests: **`PlanningUserFacingCopyTest`**, **`PhaseCPlanActionsTest`**, **`HydratePlanningSessionActionTest`**, **`IntakeBlockingClarificationTest`**, **`PlanningPromptFormatterTest`**, **`PlanCritiqueSupportTest`**, **`PlanReadinessEvaluatorTest`**, **`RouterTest`** (where routing assertions changed). Specs: **workflow-registry**; **contracts** and **mkdoc** dossier updated.

- **Luna feature-room handoff:** After provisioning, **`luna_cursor`** success **`done`** copy in **`config/bots.yaml`** includes a Discord thread link **`<#{{deliveryChannelId}}>`** (clickable intake/spec thread) and clearer wording for plan, approval, and launch.
- **Planning-thread delay visibility:** When Arrietty reaches the "ready to move forward" path, coordinator progress copy now says the next long step is posting the planning packet and running readiness checks, and **`post_planning_packet_thread`** posts a short preamble before packet chunks so the intake thread is not silent during that wait. Tests: **`PostPlanningPacketThreadActionTest`**, **`ExecutePlanningRoomCycleActionSummaryTest`**. Specs: **workflow-registry**.
- **Coordinator Discord copy (review pass):** **`planningRoomCycleErrorUserMessage`** plus **`config/bots.yaml`** draft-blocked template avoid raw cycle codes and variable-name hints; **`BuildPlanningThreadReviewBodyAction`** switches to a short pointer when the packet is already in-thread; **`RunPlanCritiqueAndReadinessAction`** avoids repeating repo grounding after the packet; **`PlanningUserFacingCopy`**, **`PlanningCyclePipeline`** workspace line, **`PlanningPromptFormatter`** fallbacks, and shorter pre-launch / blocked / human-decision **`post_channel_message`** bodies reduce duplicate reading and jargon.
- **Intake thread copy cleanup:** The initial orchestrator thread note in **`config/bots.yaml`** is shorter and no longer wraps awkwardly, the handoff prompt is tighter, **`SpreadPlanWorkspaceSignalsAction`** now emits a cleaner plain-language workspace summary instead of repeating "Workspace status", and **`EnsureRepoWorkspaceAction`** uses a simpler repo-ready progress line. Tests: **`SpreadPlanWorkspaceSignalsActionTest`**, **`EnsureRepoWorkspaceActionTest`**.
- **Documentation (routing + contracts):** **Router** intake/spec thread **messages** now match **interactions**—**coordinator only** when coordinator id resolves (tests: `routeWithFeatureRoomStore_intakeThreadId_message_returnsCoordinatorOnly`). **cursor-gathering** **contracts** updated for response policy, **Phase B** (**`build_insight_discovery_agenda`**, draft-before-non-blocker discovery, **`planningRepoEvidenceJson`**), **Phase C** (canonical **`PlanningIntakeStage`** / **`coordinator_intake_bootstrap`**, **`legacySpreadValue`**). **Routing** dossier (summary, how-it-works, contracts, change-log) and **architecture** runtime flow aligned.

- **Coordinator v2 ownership guards:** `arrietty_room_v2` now owns the production phase graph directly, and **`GraphWorkflowRunner`** persists independent configurable-step cursors per capability so named v2 phases can pause/resume without shared linear state. Tests: **ArriettyV2WorkflowYamlTest**, **GraphWorkflowRunnerTest**. Specs: **workflow-registry**.
- **Removal/cleanup (intake architecture):** **`Bootstrap`** registers **`start_coordinator_planning` only once**, after **`VinekeepersEngine`** construction (removed duplicate registration from **`registerLifecycleActions`**, which ran with a null engine). Dead **BUNDLED** multi-field discovery path removed (**`InsightDiscoverySupport`**, **`CaptureAndApplyDiscoveryAnswerAction`** bundled branch). Specs/config registry wording uses **`arrietty_room_v2`** as the production coordinator workflow id instead of a generic **`arrietty_room`** product id.

## 2026-03-20

- **Staged coordinator intake (Arrietty room):** Synthetic **`start_coordinator_planning`** kickoff sets payload **`coordinatorKickoff=true`** and moved coordinator intake off the main room handoff into the thread-owned Arrietty path. This entry reflects the intermediate design that introduced staged intake and critique gating; the current production flow is further refined by the 2026-03-22 draft-first updates, where **`planning_prepare`** no longer runs the old pre-draft structured-discovery loop before the first autonomous pass. **`run_plan_critique_and_readiness`** prepends a **MUST_FIX** (**`INTAKE_DISCOVERY_INCOMPLETE`**) while canonical **`PlanningIntakeStage`** is still **`GATHERING_CONTEXT`**; **`acknowledge_readiness_human_decision`** with bind **`humanReadinessProceedAck=true`** re-runs critique and forces **READY** spread/persistence after **NEEDS_HUMAN_DECISION** proceed. **`launch_cursor_run`** returns spread **`launchMessage`**, **`launchSucceeded`**, **`launchAgentId`** for YAML branching and thread copy; **`CursorCloudAdapterImpl`** honors optional **`CURSOR_HTTP_CONNECT_TIMEOUT_MS`** / **`CURSOR_HTTP_REQUEST_TIMEOUT_MS`**; **`CursorCloudRunMonitor`** isolates per-run poll failures. **`build_role_planning_thread_messages`** fills role thread bodies from plan artifacts for **`post_channel_message`** **`asRole`**. **`work-profiles.yaml`**: optional **`scope_summary`**; **`acceptance_criteria`** required. Specs: workflow-registry; **`.env.example`** documents HTTP timeout keys.
- **Pre-approval planning substance:** **`PlanCritiqueSupport`** adds **MUST_FIX** findings when the initial request, implementation outline, or validation approach text are below minimum length, so **`planReadinessStatus`** stays **NEEDS_REVISION** until drafted content is substantive (shared **`PlanningArtifactTexts`** with **`build_planning_thread_review_body`**). **`run_plan_critique_and_readiness`** spreads **`planAssumptionsSummary`** and **`planIssuesSummary`**; **`arrietty_room_v2`** pre-launch thread message includes those sections. Tests: **PlanCritiqueSupportTest**; PhaseCPlanActionsTest strings adjusted for thresholds.
- **Thread planning UX (Arrietty intake):** **`run_plan_critique_and_readiness`** merges **`planningThreadReviewBody`** / **`planningThreadReviewBuildError`** (same as **`build_planning_thread_review_body`**) into workflow state so **`arrietty_room_v2`** pre-launch **`post_channel_message`** can show drafted outline/validation/context before approval buttons; structured pre-launch copy in **`config/bots.yaml`**. **VinekeepersEngine** sends a minimal follow-up when a **deferred** Discord **interaction** has no workflow/reasoner outbound (avoids “interaction failed”). **DiscordAppReplySink** falls back to **OutboundDeliveryRouter** default gateway when lifecycle-resolved gateway is null. **`PlanReadinessEvaluator`** notes omit duplicate readiness label (use **`planReadinessStatus`** in copy). **Router:** intake-thread **interaction** events route to the primary coordinator only; **JdaDiscordGateway** sets **`threadId`** on interactions in **ThreadChannel** so **`sessionKeyStrategy: thread`** matches message sessions. Specs: workflow-registry, connectors-registry; tests: VinekeepersEngineTest, PhaseCPlanActionsTest, DiscordAppReplySinkTest.
- **Proposal-driven planning (Arrietty intake):** **`generate_planning_proposals`**, **`apply_auto_planning_proposals`**, **`build_proposal_confirm_prompt`**, **`resolve_proposal_confirmation`**; **`PlanningProposal`** model and **`PlanningDraftSupport`** shared with **`synthesize_plan_drafts`**. The current production **`arrietty_room_v2`** path is draft-first: **`accept_pending_planning_confirmations`** applies remaining **`CONFIRM`** rows without a user OK loop before the first autonomous pass, while the legacy confirm-prompt actions remain available for non-production or test flows. **Open questions** are no longer confirmed as an editable draft; they are gathered through coordinator clarification one at a time and resolve to **`None — ready to implement`** when nothing remains. Specs: workflow-registry, state-registry; tests: PlanningProposalActionsTest, PlanningPacketDepthEvaluatorTest, PlanCritiqueSupportTest.

## 2026-03-19

- **Repo workspace hardening + Arrietty progress:** **RepoWorkspaceService** ensures **VINEKEEPERS_REPO_WORKSPACE_ROOT** exists, removes an existing per-context clone directory before **git clone --depth 1** (retry once on delete failure; fails closed if removal incomplete), captures git output on clone failure, logs milestones, and accepts **RepoWorkspaceProgressCallback**. **EnsureRepoWorkspaceAction** wires **OutboundDeliveryRouter** and posts short **arrietty** lines to the intake thread (**deliveryChannelId**, **THREAD_CREATE_FAILED** → **channelId**) during **ensure_repo_workspace**. Types: **RepoWorkspaceProgressPhase**, **RepoWorkspaceProgressCallback**, **ExplicitBotSender**. Specs: state-registry, workflow-registry; tests: RepoWorkspaceServiceTest, EnsureRepoWorkspaceActionTest.
- **Phase C (pre-launch gate):** **`run_plan_critique_and_readiness`**, **`persist_plan_approval`**, **`PlanCritiqueSnapshot`** / **`PlanCritiqueFinding`**, **`PlanReadinessEvaluator`**, **`PlanReadinessStatus`**, **`PlanApprovalStatus`**; the coordinator-owned planning path (now shipped as **`arrietty_room_v2`** after Luna handoff) branches on **`planReadinessStatus`**, handles review choices in the intake thread, and enforces the **`launch_cursor_run`** approval gate (env **`CURSOR_LAUNCH_SKIP_APPROVAL_GATE`**). Specs: REQ-LUNA-001, REQ-STATE-001, workflow/state registries; tests: PhaseCPlanActionsTest, PlanReadinessEvaluatorTest, LaunchCursorRunActionTest.
- **Phase B (mk sync):** **cursor-gathering** summary, **key assets**, **contracts**, **tests**, and **decisions** aligned with structured discovery loop, Luna workflow actions, and REQ-LUNA-001 / REQ-STATE-001 discovery models.
- **Mk / docs sync:** Routing dossier **how-it-works** and **contracts** aligned with `Router.route()` evaluation order (filter collection, then feature-room overrides, then lifecycle). **architecture** runtime flow and **state** feature pages updated for **FeatureRoomStateStore** lookup keys and coordinator resolution.

## 2026-03-18

- **Work-profile bridge in luna_cursor:** After **ensure_repo_workspace**, workflow may run **upsert_artifact_section_data** and **get_profile_missing_fields** (see `config/work-profiles.yaml`, **`profileMissingSummary`** in thread content). Registry assets: ASSET-UPSERT-ARTIFACT-SECTION-DATA-ACTION, ASSET-GET-PROFILE-MISSING-FIELDS-ACTION.
- **Step 1 follow-up (post_channel_message target, featureId/featureSlug, participant order):** **post_channel_message** supports explicit **target: room | thread** or **targetChannelId** (bind then state). **initialize_feature_room_state** generates **featureId** (feat- + 12 hex) and **featureSlug** (from initialRequest/repo sanitized) when missing; preserves when supplied. **FeatureRoomStateStore.getParticipantBotIds** returns participant configuredBotIds in stable **PlanningRole.ordinal()** order. Contracts and README updated.
- **Arrietty-only feature room:** **provision_room_participants** now provisions only the Arrietty coordinator into state; **initialize_feature_room_state** persists the single-participant room contract in **FeatureRoomStateStore**. **CreateRoomRequest** still supports optional **participantBotIds**, but production planning rooms now use **`participantBotIds: [arrietty]`**. **Router** and **OutboundDeliveryRouter** continue resolving coordinator-only room and intake-thread delivery from **FeatureRoomStateStore** while the planning workflow itself collapses architect/auditor/scribe drafting into an Arrietty-owned pass. Tests: FeatureRoomStateStoreTest, ProvisionRoomParticipantsActionTest, InitializeFeatureRoomStateActionTest, RouterTest (feature room routing), OutboundDeliveryRouterTest (sendAsRole), PostChannelMessageActionTest (asRole), ArriettyV2WorkflowYamlTest.

## 2026-03-13

- **Capability Result Refinement:** **SpaceOperations** returns typed **CreateRoomResult**/ **CreateThreadResult**; **create_channel** and **create_thread** translate results to id or sentinel for `run()` (storeIn and branch unchanged). Cursor-gathering and related docs updated.
- **Capability contract refinement:** **create_channel** and **create_thread** build **CreateRoomRequest** and **CreateThreadRequest** via their static **from(Event, state, bind)** factories; request DTOs own bind-then-state resolution. **DiscordSpaceOperations** consumes only request getters. Docs: connectors/discord and workflow-steps contracts updated; new tests CreateRoomRequestTest, CreateThreadRequestTest.
- **Workflow Capability Seam Hardening and Request Shaping:** **create_channel** and **create_thread** now build **CreateRoomRequest** and **CreateThreadRequest** from event/state/bind and resolve **SpaceOperations** via **WorkflowCapabilitySupport.sourcePrefix(event)**. Null/blank or unregistered source prefix returns CHANNEL_CREATE_FAILED or THREAD_CREATE_FAILED (fail-closed, no Discord default). **SpaceOperations** uses request-based API; **DiscordSpaceOperations** implements for Discord. See WorkflowCapabilitySupport, CreateRoomRequest, CreateThreadRequest; updated CreateChannelAction, CreateThreadAction, DiscordSpaceOperations.
- **Workflow Action Connector Capability Extraction:** **create_channel** and **create_thread** now delegate to **SpaceOperations** by source prefix via **SpaceOperationsRegistry.get(prefix)**; **DiscordSpaceOperations** implements createRoom and createThread for Discord. **Bootstrap** registers the Discord SpaceOperations capability with the registry so lifecycle room flows (e.g. Luna, Arrietty) resolve Discord ops when event source is Discord. CreateChannelAction and CreateThreadAction no longer contain Discord-specific logic; tests: DiscordSpaceOperationsTest, SpaceOperationsRegistryTest; CreateChannelActionTest and CreateThreadActionTest updated for delegate behavior.
- **Arrietty room UX and message-first capture:** Arrietty template in `config/bots.yaml` uses `workflowRef: arrietty_room` with **message-first** flow: first step is `capture_field` (e.g. `storeIn: roomAction`) with optional **trimAndLower: true** for room name UX (trim and lowercase user input). Router ownership warning when lifecycle owner lacks handlesOwnedSpaces. No hardcoded bot ids in Router or engine.
- **Arrietty thread-based updates:** Routing fix for delivery by target (channel or thread id). **CreateThreadAction** (`create_thread` step): bind `channelId`, `threadName`; `storeIn` (e.g. `deliveryChannelId`) stores thread id or `THREAD_CREATE_FAILED`. **LifecycleContext** and **CreateLifecycleContextAction** support optional **deliveryChannelId** (excludes sentinel). **PostChannelMessageAction** send target = `deliveryChannelId` or `channelId` (bind then state); **LaunchCursorRunAction** passes `deliveryChannelId` into **LifecycleRunRecord**. **CursorCloudRunMonitor** sends status/feedback to the thread when `deliveryChannelId` is set (messageId null). **OutboundDeliveryRouter** resolves sender by delivery target id (thread or channel). Arrietty workflow config may include `create_thread` with `storeIn: deliveryChannelId` and optional branch on `THREAD_CREATE_FAILED`.
- **Arrietty UX refinement:** **create_thread** runs **after create_lifecycle_context** in the provisioning sequence (e.g. in `luna_cursor`); CreateThreadAction uses the **lifecycle owner gateway** when the channel has a lifecycle context. **LifecycleContext.withDeliveryChannelId** and **LifecycleContextStore.setDeliveryTargetId** (contextId, threadId) update the delivery target after thread creation; setDeliveryTargetId ignores null, blank, and THREAD_CREATE_FAILED. **Done step** may use a short **main-room redirect** message (e.g. `"Launching now. See <#{{channelId}}>."`).

## 2026-03-12

- **CreateChannelAction lifecycle owner and permission overwrite:** `create_channel` step bind may include `lifecycleOwnerBotId`. After creating the channel via gateway `createTextChannel`, the action calls `addPermissionOverride` for that bot's Discord user (resolved via router `getSelfUserIdForBot(lifecycleOwnerBotId)`) when the gateway supports it, so the lifecycle room bot (e.g. Arrietty) has explicit permission on the new channel. Room naming and sentinel `CHANNEL_CREATE_FAILED` unchanged. See CreateChannelActionTest.runWithRouterAndLifecycleOwnerBotId_addsPermissionOverwriteAfterCreate.

## 2026-03-10

- **Phase 1 correctness (docs):** Bind precedence for call_action (bind overrides state). Merged interpolation for `post_channel_message`: content uses state then bind (bind overrides), e.g. `lifecycleBotName` in bind in `config/bots.yaml` for room intro. `create_channel` normalizes channel name to Discord-safe before gateway create; returns `CHANNEL_CREATE_FAILED` on failure. `launch_cursor_run` acknowledgement includes optional status (e.g. "Status: launching"). README and workflow-steps/cursor-gathering/Discord docs updated.
- **Phase 1 completion:** Luna workflow now uses the full provisioning sequence: `create_channel` → branch on `CHANNEL_CREATE_FAILED` → `provision_bot_instance` → `create_lifecycle_context` → `post_channel_message` → `launch_cursor_run`. `launch_cursor_run` is the authoritative launch path; `cursor.fullRun` is not used in luna_cursor. **CursorInstructionComposer** is the single source for the Cursor run prompt (includes /nova-code). Room naming: when channel name is blank, it is derived from state (project + codeChange). `create_channel` returns sentinel `CHANNEL_CREATE_FAILED` on gateway failure. **LifecycleContext** extended with `configuredBotId`, `runtimeBotInstanceId`, optional `repo`/`requestText`. **Provision_bot_instance** uses generic instance id. README and impacted docs updated.

## 2026-03-09

- **Lifecycle room Phase 1:** Added `LifecycleContext`, `LifecycleContextStore`, `RuntimeBotInstance`; workflow actions `create_channel`, `post_channel_message`, `provision_bot_instance`, `create_lifecycle_context`, `launch_cursor_run`. Arrietty bot template in config for per-channel lifecycle room instances; Discord gateway `createTextChannel(guildId, channelName)` for channel creation.
- **Cursor API error visibility:** CursorCloudAdapterImpl constructor masks API key in logs; transport/send logging added for diagnostics without leaking key or body. CursorCloudException message is surfaced to callers (e.g. CursorCloudAdapterImplTest.transportExceptionSurfacesMessage); CursorCloudTransport throws Exception so adapter can wrap and expose cause.
- **Cursor adapter bug-fix:** Robust non-2xx error parsing (multiple response shapes: nested error.message/code, top-level message, plain text, empty body); safe per-request diagnostics at DEBUG (URI, key configured, model, repo, branch); JSON request body uses NON_NULL; CursorCloudAdapterImplTest extended with error-shape and auth tests. Auth tests (Bearer token for getAgent and launchAgent); non-2xx empty JSON object and non-JSON body tests (non2xxEmptyJsonObjectSurfacesGenericMessage, non2xxNonJsonBodySurfacesInException) added.
- **Edit-reprompt and config alignment:** Luna and configured workflow use `ConfigurableWorkflowRunner` and `ConfigurableWorkflowState`; `StepResult.clearKeys` supports edit-reprompt so state keys can be cleared before re-prompting. `config/bots.yaml` and workflow runner/state assets updated for consistency.
- **REQ-LUNA-001 acceptance (confirmation-branch clear):** Confirmation step (Launch / Edit repo / Edit request / Cancel) clears branch/state when the user chooses Edit repo or Edit request so the flow returns to repo or request input without carrying prior confirmation state.
- **Luna discordAuthors:** Luna routing in `config/bots.yaml` can include optional `discordAuthors` (e.g. `novawilde13_72571`) so only listed Discord users can trigger Luna when they mention the bot; gateway and NormalizedEventContext supply author data for routing.

## Prior

- Renamed the documented capability from a Luna-only dossier to the spec-defined `cursor-gathering` feature.
- Captured the configured `luna_cursor` flow, Cursor adapter handoff, and Discord reply path as one feature.

