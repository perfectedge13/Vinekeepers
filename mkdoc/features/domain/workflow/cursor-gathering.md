# Cursor-backed gathering workflow

# Status

active

# Summary

Luna bot gathering workflow and Cursor Cloud execution (REQ-LUNA-001). The shipped `luna` bot stays config-driven in `config/bots.yaml`, activates from `discordMention: luna` (and optional `discordAuthors`), runs configured workflow `luna_cursor` to gather repository and feature inputs across multiple Discord turns, then uses the **full provisioning sequence** for launch. **Guided repo selection:** the first step uses `choiceProvider: githubRepos` to present GitHub repos (from GitHub API, sort=updated), "Use last repo", and "Custom repo"; last repo is stored per author after a successful launch. A **confirmation step** (Launch / Edit repo / Edit request / Cancel) runs before the provisioning sequence. **After Launch**, the flow may include **provision_room_participants** (storeIn: featureRoomParticipants) and **initialize_feature_room_state** before or after channel/thread creation for multi-bot feature rooms; **initialize_feature_plan_state** and **ensure_repo_workspace** run after room state init in `luna_cursor` (plan aggregate + repo workspace linkage). **`upsert_artifact_section_data`** and **`get_profile_missing_fields`** follow **ensure_repo_workspace** in `luna_cursor` to populate **`FeaturePlanState.artifacts`** and **`profileMissingSummary`** (plain-English summaries). Luna then **`post_channel_message`**s a handoff into the intake thread, **`start_coordinator_planning`** runs the coordinator workflow once in that thread (synthetic in-process Discord message; same session key as later user replies), and **`done`**s in the main channel. **Phase B/C** run in **`arrietty_room`** from that kickoff and when the user messages that thread: **`hydrate_planning_session`** (storeSpread when the event channel matches **`FeatureRoomState.intakeThreadId`**) branches on **`planningIntakeThread`**; **`generate_planning_proposals`** / **`apply_auto_planning_proposals`** / confirm loop (**`build_proposal_confirm_prompt`**, **`resolve_proposal_confirmation`**) then **`get_structured_discovery_gaps`** / **`build_discovery_agenda`** / **`capture_and_apply_discovery_answer`** / **`classify_assumption_or_issue`** / **`recompute_plan_progress`** until gaps clear or **`discoveryBlockingIssueMode`**; gaps may carry **`userFacingDetail`** for safe prompts. **`synthesize_plan_drafts`** remains for legacy YAML. **Phase C:** **`run_plan_critique_and_readiness`**, approval choices, **`persist_plan_approval`**, role **`post_channel_message`** steps, then **`launch_cursor_run`** (gated on **`PlanApproval`** when **`contextId`** is set unless **`CURSOR_LAUNCH_SKIP_APPROVAL_GATE=true`**). Arrietty uses **`conversationMode: conversational`** so thread-scoped workflows return a stable idle reply after terminal **done** instead of restarting. The Luna provisioning sequence still runs: `create_channel` (builds **CreateRoomRequest** via request factory; resolves **SpaceOperations** via **WorkflowCapabilitySupport.sourcePrefix(event)** — fail-closed) → branch on `channelId == CHANNEL_CREATE_FAILED` → `provision_bot_instance` → `create_lifecycle_context` → `create_thread` (**CreateThreadRequest**; **DiscordSpaceOperations** uses request getters only) → room/plan/repo steps → thread handoff. **`launch_cursor_run` is the authoritative launch path** for the feature (thread workflow); `cursor.fullRun` is not used in `luna_cursor`. The Cursor run prompt is built by **`CursorInstructionComposer`** (single source; includes /nova-code). `LifecycleContext` is extended with `configuredBotId`, `runtimeBotInstanceId`, and optional `repo`/`requestText`. Vinekeepers stores `LifecycleRunRecord` and `luna:lastRepo:{authorId}`, polls Cursor, and relays launch/progress/PR updates back to Discord. The **Arrietty** bot is the template for per-channel lifecycle room instances; `RuntimeBotInstance` uses a generic instance id. The **arrietty_room** workflow is **message-first**: first step is `capture_field` (e.g. `storeIn: roomAction`, `trimAndLower: true`), then a `branch` step routes by value (status, retry, close, blank, else) to the appropriate done step. In the Luna provisioning sequence, **create_thread** runs **after create_lifecycle_context**; CreateThreadAction uses the **lifecycle owner gateway** when the channel has a context and updates **LifecycleContextStore** via **setDeliveryTargetId**(contextId, threadId); **LifecycleContext.withDeliveryChannelId** supplies the delivery target for the store. Arrietty can use a **create_thread** step (bind `channelId`, `threadName`; `storeIn: deliveryChannelId`) so Cursor updates are delivered to the thread. The **done step** may use a short **main-room redirect** message (e.g. `"Launching now. See <#{{channelId}}>."`). `LifecycleRunRecord` and `CreateLifecycleContextAction` support optional **deliveryChannelId** (excludes `THREAD_CREATE_FAILED`); **PostChannelMessageAction** send target is `deliveryChannelId` or `channelId` (bind then state); **CursorCloudRunMonitor** sends status/feedback to the thread when `deliveryChannelId` is set (messageId null).

# Key assets

| Asset | Role | Path |
|-------|------|------|
| ASSET-LUNA-STATE | Luna per-conversation state (project, code change, step) | src/main/java/com/vinekeepers/workflow/GatheringState.java |
| ASSET-LUNA-WORKFLOW | Luna workflow class used by tests while production Luna uses configured workflow `luna_cursor` | src/main/java/com/vinekeepers/workflow/CursorCloudGatheringWorkflow.java |
| ASSET-CURSOR-ADAPTER | Cursor Cloud Agents API adapter interface | src/main/java/com/vinekeepers/core/cursor/CursorCloudAdapter.java |
| ASSET-CURSOR-ADAPTER-IMPL | Cursor Cloud Agents API HTTP client implementation (env-based config); robust non-2xx error parsing (extractErrorMessageAndCode for error string, nested error.message/code, top-level message, plain text, empty); safe request diagnostics (debug URI, key masked, model, repo, branch); NON_NULL payload serialization; Bearer auth; logs transport exceptions and config without leaking key or body | src/main/java/com/vinekeepers/core/cursor/CursorCloudAdapterImpl.java |
| ASSET-LUNA-RUN-STATE | Generic run record for lifecycle Cursor run (indexed by channelId, externalRunId); optional deliveryChannelId for thread delivery; Discord reply target | src/main/java/com/vinekeepers/core/cursor/LifecycleRunRecord.java |
| ASSET-CURSOR-RUN-MONITOR | Poll Cursor status/conversation and relay updates to Discord; when LifecycleRunRecord has deliveryChannelId set, sends to that thread (messageId null) | src/main/java/com/vinekeepers/core/cursor/CursorCloudRunMonitor.java |
| ASSET-CURSOR-AGENT-LAUNCH-REQUEST | Request payload model for launching a Cursor Cloud agent run | src/main/java/com/vinekeepers/core/cursor/CursorAgentLaunchRequest.java |
| ASSET-CURSOR-AGENT-CONVERSATION | Conversation response model for Cursor Cloud agent feedback | src/main/java/com/vinekeepers/core/cursor/CursorAgentConversation.java |
| ASSET-CURSOR-AGENT-DETAILS | Details model for Cursor Cloud agent status lookups | src/main/java/com/vinekeepers/core/cursor/CursorAgentDetails.java |
| ASSET-CURSOR-AGENT-LAUNCH-RESULT | Launch result model returned after starting a Cursor Cloud agent run | src/main/java/com/vinekeepers/core/cursor/CursorAgentLaunchResult.java |
| ASSET-CURSOR-AGENT-MESSAGE | Message model representing Cursor Cloud agent conversation entries | src/main/java/com/vinekeepers/core/cursor/CursorAgentMessage.java |
| ASSET-CURSOR-CLOUD-TRANSPORT | HTTP transport abstraction for Cursor Cloud adapter calls | src/main/java/com/vinekeepers/core/cursor/CursorCloudTransport.java |
| ASSET-CURSOR-CLOUD-TRANSPORT-RESPONSE | Transport response model used by Cursor Cloud adapter calls | src/main/java/com/vinekeepers/core/cursor/CursorCloudTransportResponse.java |
| ASSET-CURSOR-CLOUD-EXCEPTION | Exception type for Cursor Cloud transport and adapter failures | src/main/java/com/vinekeepers/core/cursor/CursorCloudException.java |
| ASSET-CURSOR-FULL-RUN-TOOL | Tool wrapper that launches a Cursor Cloud Agent run and stores launch state and last repo per author | src/main/java/com/vinekeepers/tools/CursorFullRunTool.java |
| ASSET-GITHUB-REPOS-CHOICE-PROVIDER | Fetches user repos from GitHub API for guided repo selection; uses GITHUB_TOKEN | src/main/java/com/vinekeepers/providers/GitHubReposChoiceProvider.java |
| ASSET-LIFECYCLE-CONTEXT | Lifecycle run context (channelId, configuredBotId, runtimeBotInstanceId, optional repo/requestText, optional deliveryChannelId); withDeliveryChannelId(id) for store updates after create_thread | src/main/java/com/vinekeepers/state/LifecycleContext.java |
| ASSET-LIFECYCLE-CONTEXT-STORE | Store and resolve lifecycle contexts by key; setDeliveryTargetId(contextId, deliveryTargetId) used by create_thread after success (ignores null/blank/sentinel) | src/main/java/com/vinekeepers/state/LifecycleContextStore.java |
| ASSET-RUNTIME-BOT-INSTANCE | Runtime instance of a bot provisioned from a template (e.g. Arrietty); instanceId, templateBotId, displayName, channelId | src/main/java/com/vinekeepers/bot/RuntimeBotInstance.java |
| ASSET-CREATE-CHANNEL-ACTION | Workflow action that builds CreateRoomRequest via request factory (explicit intent fields, optional participantBotIds); resolves SpaceOperations by WorkflowCapabilitySupport.sourcePrefix(event); null/blank or unregistered prefix returns CHANNEL_CREATE_FAILED (fail-closed) | src/main/java/com/vinekeepers/workflow/actions/CreateChannelAction.java |
| ASSET-POST-CHANNEL-MESSAGE-ACTION | Workflow action to post a message to a Discord channel; send target = deliveryChannelId or channelId (bind then state), optional target room/thread/targetChannelId; optional asBotId or asRole with precedence asBotId over asRole; explicit sender uses sendAsExplicit/sendAsRoleExplicit (strict errors) | src/main/java/com/vinekeepers/workflow/actions/PostChannelMessageAction.java |
| ASSET-PROVISION-ROOM-PARTICIPANTS-ACTION | Workflow action to provision four feature-room participants (ORCHESTRATOR, ARCHITECT, AUDITOR, SCRIBE) into StateStore under a configurable prefix; returns list of participant maps for downstream initialize_feature_room_state | src/main/java/com/vinekeepers/workflow/actions/ProvisionRoomParticipantsAction.java |
| ASSET-INITIALIZE-FEATURE-ROOM-STATE-ACTION | Workflow action to build FeatureRoomState from transient featureRoomParticipants maps; validates role contract and persists typed RoomParticipant list | src/main/java/com/vinekeepers/workflow/actions/InitializeFeatureRoomStateAction.java |
| ASSET-GENERATE-PLANNING-PROPOSALS-ACTION | Workflow action generate_planning_proposals — AUTO_APPLY vs CONFIRM drafts from workspace readiness | src/main/java/com/vinekeepers/workflow/actions/GeneratePlanningProposalsAction.java |
| ASSET-APPLY-AUTO-PLANNING-PROPOSALS-ACTION | Workflow action apply_auto_planning_proposals — persists AUTO_APPLY proposals | src/main/java/com/vinekeepers/workflow/actions/ApplyAutoPlanningProposalsAction.java |
| ASSET-BUILD-PROPOSAL-CONFIRM-PROMPT-ACTION | Workflow action build_proposal_confirm_prompt — plain-English confirm prompt | src/main/java/com/vinekeepers/workflow/actions/BuildProposalConfirmPromptAction.java |
| ASSET-RESOLVE-PROPOSAL-CONFIRMATION-ACTION | Workflow action resolve_proposal_confirmation — OK vs edited upsert | src/main/java/com/vinekeepers/workflow/actions/ResolveProposalConfirmationAction.java |
| ASSET-STRUCTURED-DISCOVERY-SUPPORT | Collects DiscoveryGap list from plan + profile, builds DiscoveryAgenda/questions, serializes branch flags and section status projection for workflow state | src/main/java/com/vinekeepers/workflow/discovery/StructuredDiscoverySupport.java |
| ASSET-GET-STRUCTURED-DISCOVERY-GAPS-ACTION | Workflow action get_structured_discovery_gaps — storeSpread for gaps JSON, discoveryHasOpenGaps, discoveryBlockingIssueMode | src/main/java/com/vinekeepers/workflow/actions/GetStructuredDiscoveryGapsAction.java |
| ASSET-BUILD-DISCOVERY-AGENDA-ACTION | Workflow action build_discovery_agenda — first prioritized question and apply-target keys into state (storeSpread) | src/main/java/com/vinekeepers/workflow/actions/BuildDiscoveryAgendaAction.java |
| ASSET-CAPTURE-APPLY-DISCOVERY-ANSWER-ACTION | Workflow action capture_and_apply_discovery_answer — applies discoveryAnswerRaw when discoveryApplyKind is REQUIRED_FIELD | src/main/java/com/vinekeepers/workflow/actions/CaptureAndApplyDiscoveryAnswerAction.java |
| ASSET-CLASSIFY-ASSUMPTION-OR-ISSUE-ACTION | Workflow action classify_assumption_or_issue — rule-based append_plan_issue vs append_plan_assumption from answer text | src/main/java/com/vinekeepers/workflow/actions/ClassifyAssumptionOrIssueAction.java |
| ASSET-RECOMPUTE-PLAN-PROGRESS-ACTION | Workflow action recompute_plan_progress — refresh section statuses and re-spread gap flags for loop exit | src/main/java/com/vinekeepers/workflow/actions/RecomputePlanProgressAction.java |
| ASSET-ENSURE-REPO-WORKSPACE-ACTION | Workflow action ensure_repo_workspace — materialize or resolve repo workspace; link FeaturePlanState; optional Arrietty progress posts | src/main/java/com/vinekeepers/workflow/actions/EnsureRepoWorkspaceAction.java |
| ASSET-SPREAD-PLAN-WORKSPACE-SIGNALS-ACTION | Workflow action spread_plan_workspace_signals — repoWorkspaceReady and status summary for YAML branches | src/main/java/com/vinekeepers/workflow/actions/SpreadPlanWorkspaceSignalsAction.java |
| ASSET-OPENAI-CHAT-CLIENT | OpenAI-compatible chat/completions HTTP client for optional planning synthesis | src/main/java/com/vinekeepers/connectors/openai/OpenAiChatClient.java |
| ASSET-RUN-LLM-PLANNING-SYNTHESIS-ACTION | Workflow action run_llm_planning_synthesis — JSON upserts and follow-up questions spread | src/main/java/com/vinekeepers/workflow/actions/RunLlmPlanningSynthesisAction.java |
| ASSET-EXECUTE-PLANNING-ROOM-CYCLE-ACTION | Workflow action execute_planning_room_cycle — iterative architect/auditor/scribe passes, depth retries, question ranking | src/main/java/com/vinekeepers/workflow/actions/ExecutePlanningRoomCycleAction.java |
| ASSET-MERGE-PLANNING-CLARIFICATION-CHOICE-ACTION | Workflow action merge_planning_clarification_choice — applies clarification choice to assumptions and decision log | src/main/java/com/vinekeepers/workflow/actions/MergePlanningClarificationChoiceAction.java |
| ASSET-EVALUATE-PLANNING-APPROVAL-GATE-ACTION | Workflow action evaluate_planning_approval_gate — planningReadyForApproval spread | src/main/java/com/vinekeepers/workflow/actions/EvaluatePlanningApprovalGateAction.java |
| ASSET-PLANNING-CLARIFICATION-CHOICE-PROVIDER | choiceProvider planningClarification — dynamic buttons from planningClarificationChoicesJson | src/main/java/com/vinekeepers/providers/PlanningClarificationChoiceProvider.java |
| ASSET-PLANNING-PACKET-DEPTH-EVALUATOR | Heuristic depth check before posting full planning packet | src/main/java/com/vinekeepers/workflow/planreview/PlanningPacketDepthEvaluator.java |
| ASSET-EVALUATE-PLANNING-PACKET-DEPTH-ACTION | Workflow action evaluate_planning_packet_depth — planningPacketDepthOk spread | src/main/java/com/vinekeepers/workflow/actions/EvaluatePlanningPacketDepthAction.java |
| ASSET-PROVISION-BOT-INSTANCE-ACTION | Workflow action to provision a runtime bot instance (e.g. Arrietty) bound to a channel | src/main/java/com/vinekeepers/workflow/actions/ProvisionBotInstanceAction.java |
| ASSET-CREATE-LIFECYCLE-CONTEXT-ACTION | Workflow action to create and store a lifecycle context for a run; reads optional deliveryChannelId from bind/state (excludes THREAD_CREATE_FAILED) | src/main/java/com/vinekeepers/workflow/actions/CreateLifecycleContextAction.java |
| ASSET-CREATE-THREAD-ACTION | Workflow action that builds CreateThreadRequest via request factory (explicit intent fields), resolves SpaceOperations by WorkflowCapabilitySupport.sourcePrefix(event); null/blank or unregistered prefix returns THREAD_CREATE_FAILED (fail-closed); on success when contextId present updates LifecycleContextStore via setDeliveryTargetId | src/main/java/com/vinekeepers/workflow/actions/CreateThreadAction.java |
| ASSET-SPACE-OPERATIONS | Interface: createRoom(CreateRoomRequest), createThread(CreateThreadRequest); resolved by source prefix from SpaceOperationsRegistry; fail-closed when no ops for prefix | src/main/java/com/vinekeepers/connectors/SpaceOperations.java |
| ASSET-SPACE-OPERATIONS-REGISTRY | Thread-safe registry of SpaceOperations by connector id (source prefix); Bootstrap registers Discord SpaceOperations | src/main/java/com/vinekeepers/connectors/SpaceOperationsRegistry.java |
| ASSET-DISCORD-SPACE-OPERATIONS | Implements SpaceOperations for Discord; uses only request getters (no raw map access); createRoom/createThread (createTextChannel, addPermissionOverride, createThreadChannel, setDeliveryTargetId) | src/main/java/com/vinekeepers/connectors/DiscordSpaceOperations.java |
| ASSET-LAUNCH-CURSOR-RUN-ACTION | Workflow action to launch a Cursor cloud run and register run record; passes optional deliveryChannelId into LifecycleRunRecord (authoritative launch path for luna_cursor) | src/main/java/com/vinekeepers/workflow/actions/LaunchCursorRunAction.java |
| ASSET-CURSOR-INSTRUCTION-COMPOSER | Single source for Cursor run instruction text (repo, baseBranch, change; includes /nova-code) | src/main/java/com/vinekeepers/core/cursor/CursorInstructionComposer.java |
| ASSET-BOTS-YAML | Bot definitions YAML including Luna routing and configured workflow reference | config/bots.yaml |
| ASSET-BOOTSTRAP | Register workflow runners, tools, adapter dependencies, and plan/discovery actions (hydrate_planning_session, synthesize_plan_drafts, critique/approval, etc.) | src/main/java/com/vinekeepers/core/Bootstrap.java |
| ASSET-ENGINE | Deliver workflow replies to Discord when the source is Discord | src/main/java/com/vinekeepers/core/VinekeepersEngine.java |

# Sub-pages

- [How it works](cursor-gathering/how-it-works.md)
- [Change log](cursor-gathering/change-log.md)
- [Known issues](cursor-gathering/known-issues.md)
- [Decisions](cursor-gathering/decisions.md)
- [Contracts](cursor-gathering/contracts.md)
- [Tests](cursor-gathering/tests.md)
- [Diagrams](cursor-gathering/diagrams.md)

