# Known issues

# Current limitations

- The DSL is limited to the built-in step types and does not document a general looping or sub-workflow mechanism.
- Tool-backed actions still depend on the runner for execution context and policy enforcement.

