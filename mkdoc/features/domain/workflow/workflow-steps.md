# Workflow step DSL and branching actions

# Status

active

# Summary

Workflow step DSL and branching actions (REQ-WORKFLOW-001, REQ-WORKFLOW-002). `WorkflowDefinition` describes configured flows; built-in step types implement prompt, capture, branching, tool-backed action, and completion. **`workflowSchema: v2`** workflows use phase graphs (`GraphWorkflowRunner`); production Arrietty uses **`arrietty_room_v2`** as a direct phase-owned lifecycle. Capability **`configurable_steps`** embeds the same linear step DSL under a v2 capability (no sibling `workflowRef` needed for production orchestration). Capability **`legacy_action`** still maps one registry action per pipeline step, and **`linear_workflow_ref`** remains available for non-Arrietty workflows that intentionally delegate to a separate sibling linear flow; production **`arrietty_room_v2`** does not use **`linear_workflow_ref`**. Runner diagnostics now log the full throwable plus phase/step, capability, and action context, and blank exception messages fall back to the exception class name so audits and user-visible replies are never empty. Optional workflow-level **`templates`** map: **`exposeInternal`** (default true) and optional **`allowedKeys`**; when false, `prompt_for_field`, `done`, and `post_channel_message` use allowlisted coordinator keys via **`WorkflowTemplateInterpolator`**. **Work profiles** (`config/work-profiles.yaml`) may declare **`readiness.anyOfGroups`** and field **`minWords`** / **`readinessChecks`** for **`GenericReadinessEvaluator`** (used with **`evaluate_planning_packet_depth`**). **Intent and dynamic choices:** `prompt_for_field` may include optional `intent` (e.g. `present_choices`, `confirm_action`), `choices`, `choiceProvider` (e.g. `githubRepos`), `confirmLabel`, `cancelLabel`, or `fields`; optional **`control.mode`** (e.g. `plain_text`). Prompt text supports `{{key}}` interpolation (full state when `exposeInternal` is true, else allowlisted keys). **Capture:** `capture_field` reads from message content or, for `kind: interaction`, normalizes payload `values` (Map or List) and reads selected value or `customId`. Optional **transforms** (list: trim, lower, upper, default). **trimAndLower: true** maps to [trim, lower]; when both trimAndLower and transforms are present, **trimAndLower wins** (backward compatibility). **extract_event:** Step type with **fromEvent** list of { from, storeIn, default?, transforms? }. Source paths: **payload.\<key\>** (flat), **context.\<field\>** (NormalizedEventContext getters); list-valued context fields (mentions, labels, interactionValues) stored as List in state. Missing path → default or skip. **Branch:** `branch` supports `when: else`, state-key truthy, or `when: { key, value, operator?, transform? }`. Operators: equals, not_equals, blank, nonblank, contains, starts_with, regex (full-string match), one_of (value: YAML list). Transforms on branch apply to state value only (trim, lower, upper). Blank or empty: use `when: { key, value: "" }` or branch operator blank/nonblank. **create_thread** and **create_channel:** Call-action step types that use request DTOs (**CreateThreadRequest**, **CreateRoomRequest**) with explicit intent fields; a request factory performs bind/state resolution; actions resolve **SpaceOperations** via **WorkflowCapabilitySupport.sourcePrefix(event)** (source prefix from event). Null or blank prefix or unregistered prefix returns `THREAD_CREATE_FAILED` or `CHANNEL_CREATE_FAILED` — **fail-closed**, no Discord default. **DiscordSpaceOperations** uses only request getters. Bind `channelId`, `threadName` (create_thread); optional `storeIn` (e.g. `deliveryChannelId`) stores the result or sentinel in state. Coordinator synthetic kickoff follows the same rule: if the workflow errors or yields no visible outbound reply, the engine marks the thread session as failed and posts a fallback internal-error notice instead of silently claiming success.

# Key assets

| Asset | Role | Path |
|-------|------|------|
| ASSET-STEP-RESULT | Result of a single workflow step execution with next-step, storage, outcome metadata, and optional OutboundResponse richReply | src/main/java/com/vinekeepers/workflow/StepResult.java |
| ASSET-STEP-OUTCOME | Outcome enum for continue, waiting, complete, and error workflow step states | src/main/java/com/vinekeepers/workflow/StepOutcome.java |
| ASSET-WORKFLOW-STEP | Single step in a configurable workflow | src/main/java/com/vinekeepers/workflow/WorkflowStep.java |
| ASSET-WORKFLOW-ACTION | Action interface invokable from workflow steps | src/main/java/com/vinekeepers/workflow/WorkflowAction.java |
| ASSET-WORKFLOW-ACTION-REGISTRY | Register and resolve workflow actions by name | src/main/java/com/vinekeepers/workflow/WorkflowActionRegistry.java |
| ASSET-WORKFLOW-DEFINITION | Workflow definition from config (id, steps, optional workflow-level llm defaults, optional templates policy) | src/main/java/com/vinekeepers/workflow/WorkflowDefinition.java |
| ASSET-ASK-FOR-INPUT-STEP | Step that asks for user input and stores in state | src/main/java/com/vinekeepers/workflow/steps/AskForInputStep.java |
| ASSET-PROMPT-FOR-FIELD-STEP | Step that prompts once for a field and pauses until a later event; optional intent/choices/choiceProvider; interpolates {{key}} from state | src/main/java/com/vinekeepers/workflow/steps/PromptForFieldStep.java |
| ASSET-CAPTURE-FIELD-STEP | Step that captures a field from the current event; optional transforms list; when both trimAndLower and transforms present, trimAndLower wins | src/main/java/com/vinekeepers/workflow/steps/CaptureFieldFromEventStep.java |
| ASSET-CALL-ACTION-STEP | Step that invokes a registered `WorkflowAction` or `ToolRunner`-backed tool with bound arguments | src/main/java/com/vinekeepers/workflow/steps/CallActionStep.java |
| ASSET-BRANCH-STEP | Step that branches by condition; when key/value with optional operator and transform (state value only); operators equals, not_equals, blank, nonblank, contains, starts_with, regex, one_of | src/main/java/com/vinekeepers/workflow/steps/BranchStep.java |
| ASSET-WORKFLOW-TRANSFORMS | Shared string transforms (trim, lower, upper, default); null/blank-safe | src/main/java/com/vinekeepers/workflow/WorkflowTransforms.java |
| ASSET-WORKFLOW-CONDITION-EVALUATOR | Evaluates branch condition with operators and optional state-value transforms | src/main/java/com/vinekeepers/workflow/WorkflowConditionEvaluator.java |
| ASSET-EXTRACT-EVENT-FIELDS-STEP | Step that extracts event fields into state; fromEvent with payload.\<key\>, context.\<field\>; list-valued as List | src/main/java/com/vinekeepers/workflow/steps/ExtractEventFieldsStep.java |
| ASSET-DYNAMIC-CHOICE-PROVIDER | Interface for dynamic choice providers used by prompt_for_field when choiceProvider is set | src/main/java/com/vinekeepers/workflow/DynamicChoiceProvider.java |
| ASSET-DONE-STEP | Step that completes workflow with optional message | src/main/java/com/vinekeepers/workflow/steps/DoneStep.java |
| ASSET-WORKFLOW-CAPABILITY-SUPPORT | Utility to resolve source prefix from event for capability lookups (e.g. SpaceOperationsRegistry); null/blank prefix yields fail-closed behavior in actions (no Discord default) | src/main/java/com/vinekeepers/workflow/WorkflowCapabilitySupport.java |
| ASSET-CREATE-CHANNEL-ACTION | Workflow action that builds CreateRoomRequest via request factory (explicit intent fields), resolves SpaceOperations by WorkflowCapabilitySupport.sourcePrefix(event); null/blank or unregistered prefix returns CHANNEL_CREATE_FAILED (fail-closed) | src/main/java/com/vinekeepers/workflow/actions/CreateChannelAction.java |
| ASSET-POST-CHANNEL-MESSAGE-ACTION | Workflow action to post a message to a Discord channel; send target = deliveryChannelId or channelId (bind then state), or explicit **target: room \| thread** or **targetChannelId**; content interpolation respects workflow template policy from state; optional asBotId or asRole (precedence asBotId over asRole); explicit sender strict via sendAsExplicit/sendAsRoleExplicit | src/main/java/com/vinekeepers/workflow/actions/PostChannelMessageAction.java |
| ASSET-POST-PLANNING-PROGRESS-IF-CHANGED-ACTION | post_planning_progress_if_changed — posts when post-worthy (fingerprint differs from last hash); optional bind keys **progressPostWorthyKey**, **progressPostFingerprintKey**, **lastProgressPostHashKey**, **progressPostSkippedKey**, **progressPostSendResultKey** default to planning-prefixed state keys | src/main/java/com/vinekeepers/workflow/actions/PostPlanningProgressIfChangedAction.java |
| ASSET-PROVISION-ROOM-PARTICIPANTS-ACTION | Workflow action to provision four feature-room participants (ORCHESTRATOR, ARCHITECT, AUDITOR, SCRIBE) into state; storeIn: featureRoomParticipants for downstream initialize_feature_room_state | src/main/java/com/vinekeepers/workflow/actions/ProvisionRoomParticipantsAction.java |
| ASSET-INITIALIZE-FEATURE-ROOM-STATE-ACTION | Workflow action to build FeatureRoomState from bind/state (featureRoomParticipants handoff, contextId, channelId) and put in FeatureRoomStateStore; role-contract validation; when featureId/featureSlug missing, generates feat- id and slug from initialRequest/repo | src/main/java/com/vinekeepers/workflow/actions/InitializeFeatureRoomStateAction.java |
| ASSET-PROVISION-BOT-INSTANCE-ACTION | Workflow action to provision a runtime bot instance (e.g. Arrietty) bound to a channel | src/main/java/com/vinekeepers/workflow/actions/ProvisionBotInstanceAction.java |
| ASSET-CREATE-LIFECYCLE-CONTEXT-ACTION | Workflow action to create and store a lifecycle context for a run | src/main/java/com/vinekeepers/workflow/actions/CreateLifecycleContextAction.java |
| ASSET-CREATE-THREAD-ACTION | Workflow action that builds CreateThreadRequest via request factory (explicit intent fields), resolves SpaceOperations by WorkflowCapabilitySupport.sourcePrefix(event); null/blank or unregistered prefix returns THREAD_CREATE_FAILED (fail-closed) | src/main/java/com/vinekeepers/workflow/actions/CreateThreadAction.java |
| ASSET-SPACE-OPERATIONS | Interface for connector-specific space operations (createRoom, createThread); resolved by source prefix from SpaceOperationsRegistry | src/main/java/com/vinekeepers/connectors/SpaceOperations.java |
| ASSET-SPACE-OPERATIONS-REGISTRY | Thread-safe registry of SpaceOperations by connector id (source prefix); register(connectorId, ops), get(connectorId) | src/main/java/com/vinekeepers/connectors/SpaceOperationsRegistry.java |
| ASSET-LAUNCH-CURSOR-RUN-ACTION | Workflow action to launch a Cursor cloud run and register run record | src/main/java/com/vinekeepers/workflow/actions/LaunchCursorRunAction.java |
| ASSET-INITIALIZE-FEATURE-PLAN-STATE-ACTION | Create FeaturePlanState with default profileId and empty artifacts from registry | src/main/java/com/vinekeepers/workflow/actions/InitializeFeaturePlanStateAction.java |
| ASSET-ENSURE-REPO-WORKSPACE-ACTION | Resolve repo workspace and link plan state; reuses prior MATERIALIZED/RESOLVED_LOCAL checkout when context+ref match (VINEKEEPERS_REPO_WORKSPACE_FORCE_REFRESH bypasses reuse) | src/main/java/com/vinekeepers/workflow/actions/EnsureRepoWorkspaceAction.java |
| ASSET-PREP-PLANNING-REPO-GROUNDING-ACTION | prep_planning_repo_grounding — after ensure_repo_workspace, loads `.vinekeepers/repo-grounding.parquet` when present, chunks checkout, embeds missing hashes via OpenAI into Qdrant (repo_ref + branch payload), exports merged Parquet, spreads planningRag* and retrieval text; no-ops or degrades when RAG disabled or misconfigured | src/main/java/com/vinekeepers/workflow/actions/PrepPlanningRepoGroundingAction.java |
| ASSET-PLANNING-RAG-GROUNDING | Planning repo RAG: PlanningRagConfig, QdrantPlanningClient (HTTP), RepoGroundingParquetStore, RepoFileChunker, GitRepoBranchResolver, GroundingChunkRecord; embedTexts via OpenAiChatClient | src/main/java/com/vinekeepers/workflow/planning/rag/PlanningRepoGroundingService.java |
| ASSET-POST-PLANNING-PACKET-THREAD-ACTION | post_planning_packet_thread — posts planning packet chunks; skips duplicate body via SHA-256 fingerprint unless bind forceRepost; spreads planningPacketSkipReason when skipped | src/main/java/com/vinekeepers/workflow/actions/PostPlanningPacketThreadAction.java |
| ASSET-MERGE-PLANNING-CLARIFICATION-CHOICE-ACTION | merge_planning_clarification_choice — assumptions, decision_log, exploration_body append; clears clarification scratch keys | src/main/java/com/vinekeepers/workflow/actions/MergePlanningClarificationChoiceAction.java |
| ASSET-UPSERT-ARTIFACT-SECTION-DATA-ACTION | upsert_artifact_section_data — merge section data into plan artifacts | src/main/java/com/vinekeepers/workflow/actions/UpsertArtifactSectionDataAction.java |
| ASSET-GET-PROFILE-MISSING-FIELDS-ACTION | get_profile_missing_fields — missing-required summary to storeIn | src/main/java/com/vinekeepers/workflow/actions/GetProfileMissingFieldsAction.java |
| ASSET-WORKFLOW-TEMPLATE-POLICY | Workflow YAML templates block → state keys __workflowTemplatesExposeInternal / __workflowTemplatesAllowedKeys | src/main/java/com/vinekeepers/workflow/template/WorkflowTemplatePolicy.java |
| ASSET-WORKFLOW-TEMPLATE-INTERPOLATOR | Legacy full-state vs allowlisted {{key}} interpolation for prompts and posts | src/main/java/com/vinekeepers/workflow/template/WorkflowTemplateInterpolator.java |
| ASSET-PLANNING-CYCLE-PIPELINE | Planning room cycle; partial entry points for planning_cycle_* actions; when **`PlanningPostDraftGovernor`** resolves **`ASK_ONE_QUESTION`**, backfills blank **`planningClarificationQuestionText`**, may persist **`PlanningIntakeStage.CLARIFYING`**, and **`applyAskOneQuestionUserVisibleCopy`** forces **`planningCycleProgressSummary`**, **`userCopyCoordinatorProgress`**, and **`planningOrchestratorRoundSummary`** to a single clarification-first line (optional stuck hint, ❓ + truncated question) so packet/review preamble does not mix into that beat | src/main/java/com/vinekeepers/workflow/planning/PlanningCyclePipeline.java |
| ASSET-PLANNING-GAP-EVALUATOR | Clarification gating: legacy OPEN ledger rehydration vs canonical_v1 gap pending; CANONICAL_V1 skips stale ledger re-ranking | src/main/java/com/vinekeepers/workflow/planning/PlanningGapEvaluator.java |
| ASSET-PLANNING-COORDINATOR-ROLE | COORDINATOR pass enum with consolidated LLM prompt block for StructuredLlmArtifactUpsertPass | src/main/java/com/vinekeepers/workflow/planning/PlanningCoordinatorRole.java |
| ASSET-CONFIGURABLE-PASS-RUNNER | Parses planningRolePassOrder into ordered PlanningCoordinatorRole list | src/main/java/com/vinekeepers/workflow/planning/ConfigurablePassRunner.java |
| ASSET-PLANNING-ROLE-PASS-RUNNER | Executes one architect/auditor/scribe pass by delegating the selected coordinator role prompt to StructuredLlmArtifactUpsertPass and returning applied-upsert/follow-up metadata | src/main/java/com/vinekeepers/workflow/planning/PlanningRolePassRunner.java |
| ASSET-PLANNING-READINESS-SPREAD | Planning-cycle explanatory readiness helper: spreads review/approval allowance keys and reasons without owning final reviewReady / approvalReady flags | src/main/java/com/vinekeepers/workflow/planning/PlanningReadinessSpread.java |
| ASSET-EXECUTE-PLANNING-ROOM-CYCLE-ACTION | execute_planning_room_cycle — full cycle facade | src/main/java/com/vinekeepers/workflow/actions/ExecutePlanningRoomCycleAction.java |
| ASSET-PLANNING-RUN-EXPANSION-PHASE-ACTION | planning_cycle_run_expansion | src/main/java/com/vinekeepers/workflow/actions/PlanningRunExpansionPhaseAction.java |
| ASSET-PLANNING-RUN-INNER-ROUND-ACTION | planning_cycle_run_inner_round | src/main/java/com/vinekeepers/workflow/actions/PlanningRunInnerRoundAction.java |
| ASSET-PLANNING-FINALIZE-PLANNING-CYCLE-ACTION | planning_cycle_finalize | src/main/java/com/vinekeepers/workflow/actions/PlanningFinalizePlanningCycleAction.java |
| ASSET-STRUCTURED-LLM-ARTIFACT-UPSERT-PASS | Generic LLM JSON upsert over plan artifacts; used by PlanningRolePassRunner and supplies OpenAiCallContext per pass for progress + logging | src/main/java/com/vinekeepers/workflow/planning/StructuredLlmArtifactUpsertPass.java |
| ASSET-LIGHTWEIGHT-PLANNING-REPLY | Normalizes short replies like `ok` / `continue` into consent-to-proceed so terse acknowledgements are not stored as literal artifact text | src/main/java/com/vinekeepers/workflow/planning/LightweightPlanningReply.java |
| ASSET-PLANNING-PLACEHOLDER-DETECTION | Shared detector for stub placeholder text and hollow exploration bodies so readiness, critique, and draft expansion ignore filler content | src/main/java/com/vinekeepers/workflow/planning/PlanningPlaceholderDetection.java |
| ASSET-RUN-REQUEST-EXPANSION-LLM-ACTION | run_request_expansion_llm — optional structured expansion before build_request_exploration; when a materialized **`repoLocalPath`** exists on the plan or **`planningRepoEvidenceJson`** is non-trivial, the user JSON adds **`planner_instruction`** steering toward **`repo_evidence_this_pass=observed`** and concrete **`impacted_components`** grounded in that snapshot; uses PlanningContentGenerator / OpenAiPlanningContentGenerator and passes OpenAiCallContext for progress/logging | src/main/java/com/vinekeepers/workflow/actions/RunRequestExpansionLlmAction.java |
| ASSET-BUILD-REQUEST-EXPLORATION-ACTION | build_request_exploration — deterministically seeds `request_exploration.analysis.exploration_body` from the initial request, repo sample files, and README hints; skips substantive existing content | src/main/java/com/vinekeepers/workflow/actions/BuildRequestExplorationAction.java |
| ASSET-RUN-LLM-PLANNING-SYNTHESIS-ACTION | run_llm_planning_synthesis — optional LLM artifact upserts; passes OpenAiCallContext for progress/logging | src/main/java/com/vinekeepers/workflow/actions/RunLlmPlanningSynthesisAction.java |
| ASSET-CLASSIFY-PLANNING-INGRESS-ACTION | classify_planning_ingress — normalizes planningIngressMode for v2 graphs from event kind, capability, awaiting field, and canonical intake stage | src/main/java/com/vinekeepers/workflow/actions/ClassifyPlanningIngressAction.java |
| ASSET-SET-PLANNING-INTAKE-STAGE-ACTION | set_planning_intake_stage — persists PlanningIntakeStage on FeaturePlanState; spreads canonicalPlanningIntakeStage | src/main/java/com/vinekeepers/workflow/actions/SetPlanningIntakeStageAction.java |
| ASSET-PREP-PLANNING-REPO-GROUNDING-ACTION | prep_planning_repo_grounding — optional Parquet/Qdrant repo RAG after ensure_repo_workspace | src/main/java/com/vinekeepers/workflow/actions/PrepPlanningRepoGroundingAction.java |
| ASSET-PLANNING-RAG-GROUNDING | Planning repo RAG orchestration (chunking, embeddings, Qdrant); continues gracefully when disabled | src/main/java/com/vinekeepers/workflow/planning/rag/PlanningRepoGroundingService.java |
| ASSET-OPENAI-CHAT-CLIENT | OpenAI-compatible chat client; optional OpenAiPlanningProgressSink; truncated redacted body logging per env | src/main/java/com/vinekeepers/connectors/openai/OpenAiChatClient.java |
| ASSET-OPENAI-CALL-CONTEXT | Event, state map, and activitySummary for one planning completion (progress + structured logs) | src/main/java/com/vinekeepers/connectors/openai/OpenAiCallContext.java |
| ASSET-OPENAI-PLANNING-PROGRESS-SINK | Callback before planning OpenAI HTTP calls (NOOP default; Bootstrap wires poster) | src/main/java/com/vinekeepers/connectors/openai/OpenAiPlanningProgressSink.java |
| ASSET-OPENAI-PLANNING-PROGRESS-POSTER | Posts **Update:** lines via post_channel_message when Discord progress env enabled | src/main/java/com/vinekeepers/workflow/actions/OpenAiPlanningProgressPoster.java |
| ASSET-GENERIC-READINESS-EVALUATOR | Declarative readiness from work profile paths (anyOf, minWords, checks) | src/main/java/com/vinekeepers/workflow/readiness/GenericReadinessEvaluator.java |
| ASSET-PLANNING-PACKET-DEPTH-EVALUATOR | Packet depth gate; delegates to GenericReadinessEvaluator when profile has declarative readiness | src/main/java/com/vinekeepers/workflow/planreview/PlanningPacketDepthEvaluator.java |
| ASSET-EVALUATE-PLANNING-PACKET-DEPTH-ACTION | evaluate_planning_packet_depth — uses plan profileId + WorkProfileRegistry | src/main/java/com/vinekeepers/workflow/actions/EvaluatePlanningPacketDepthAction.java |
| ASSET-DERIVE-PLAN-GOVERNANCE-ACTION | derive_plan_governance — merges discovery gaps and artifacts into PlanIssue / PlanRisk / PlanDecision / unresolved rows on FeaturePlanState | src/main/java/com/vinekeepers/workflow/actions/DerivePlanGovernanceAction.java |
| ASSET-PLAN-GOVERNANCE-DERIVER | Deterministic governance merge for packet, depth, and critique; de-dupes open questions vs artifact lines | src/main/java/com/vinekeepers/workflow/planning/PlanGovernanceDeriver.java |
| ASSET-PLANNING-ARTIFACT-TEXTS | Reads artifact bodies; substantiveUnresolvedQuestionLines filters packet open-question rows | src/main/java/com/vinekeepers/workflow/planreview/PlanningArtifactTexts.java |
| ASSET-PLAN-CRITIQUE-RUBRIC | Rollup of critique findings and workspace signals into dimension scores | src/main/java/com/vinekeepers/workflow/planreview/PlanCritiqueRubric.java |
| ASSET-PLAN-READINESS-CALCULATOR | PlanConfidence from evidence (readiness, confidenceScore, confidenceReasons, structuredKnownFactCount / materialUnknownCount / materialUnknownLabels); **APPROVAL_CONFIDENCE_THRESHOLD** 0.85 shared with approval gate | src/main/java/com/vinekeepers/workflow/planreview/PlanReadinessCalculator.java |
| ASSET-PLANNING-APPROVAL-GATE-SUPPORT | Shared **validateApproveAllowed** (threshold on stored confidenceScore when set) and status mapping for evaluate_planning_approval_gate / persist_plan_approval | src/main/java/com/vinekeepers/workflow/planreview/PlanningApprovalGateSupport.java |
| ASSET-PLANNING-THREAD-PACKET-FORMATTER | Full planning packet markdown from FeaturePlanState; Discord-friendly labeled lists for governance items and explicit truncation | src/main/java/com/vinekeepers/workflow/planreview/PlanningThreadPacketFormatter.java |
| ASSET-EVALUATE-PLANNING-APPROVAL-GATE-ACTION | evaluate_planning_approval_gate — planningReadyForApproval when packet, depth, READY readiness, canonical intake/clarification state, and **confidenceScore** ≥ **0.85** (when persisted) | src/main/java/com/vinekeepers/workflow/actions/EvaluatePlanningApprovalGateAction.java |
| ASSET-VISIBLE-DELTA-TRACKER | Fingerprint keyed state slice for visible-delta detection | src/main/java/com/vinekeepers/workflow/convergence/VisibleDeltaTracker.java |
| ASSET-USER-COPY-CONTEXT | Default allowlisted coordinator keys for template slicing | src/main/java/com/vinekeepers/workflow/viewmodel/UserCopyContext.java |

# Sub-pages

- [How it works](workflow-steps/how-it-works.md)
- [Change log](workflow-steps/change-log.md)
- [Known issues](workflow-steps/known-issues.md)
- [Decisions](workflow-steps/decisions.md)
- [Contracts](workflow-steps/contracts.md)
- [Tests](workflow-steps/tests.md)
- [Diagrams](workflow-steps/diagrams.md)

