# Compressed spec + mkdoc context (for update)

## Scope
Affected features: workflow/cursor-gathering, config/config (plan_change: specs/workflow-registry.yml, specs/config-registry.yml; assets: LifecycleContext, LifecycleContextStore, CreateThreadAction, Bootstrap, config/bots.yaml).

## Per feature

### workflow/cursor-gathering
- **Feature:** Cursor-backed gathering workflow, active, doc_path: features/domain/workflow/cursor-gathering.md. Luna gathers repo/feature input, launches Cursor run; Arrietty template for lifecycle room; create_thread after create_lifecycle_context; setDeliveryTargetId, withDeliveryChannelId; done step main-room short redirect.
- **Requirements:** REQ-LUNA-001 — create_channel → provision_bot_instance → create_lifecycle_context → create_thread (optional; uses lifecycle owner gateway; setDeliveryTargetId when contextId present) → post_channel_message → launch_cursor_run. LifecycleContext.withDeliveryChannelId; LifecycleContextStore.setDeliveryTargetId (ignores null/blank/sentinel). Done step may use main-room redirect message (e.g. "Launching now. See <#{{channelId}}>.").
- **Assets:** ASSET-LIFECYCLE-CONTEXT (withDeliveryChannelId), ASSET-LIFECYCLE-CONTEXT-STORE (setDeliveryTargetId), ASSET-CREATE-THREAD-ACTION (bind channelId, threadName, optional contextId; owner gateway when channel has context; setDeliveryTargetId on success), ASSET-CREATE-LIFECYCLE-CONTEXT-ACTION, ASSET-BOOTSTRAP, ASSET-BOTS-YAML, ASSET-POST-CHANNEL-MESSAGE-ACTION, ASSET-LAUNCH-CURSOR-RUN-ACTION, etc.
- **Tests:** CreateThreadActionTest (owner gateway when channel has context, setDeliveryTargetId on success when contextId present, no store update on failure, blank channelId returns THREAD_CREATE_FAILED), LifecycleContextTest, LifecycleContextStoreTest, CreateLifecycleContextActionTest, LaunchCursorRunActionTest, etc.

### config/config
- **Feature:** Bot config from YAML, active, doc_path: features/domain/config/config.md. ConfigLoader loads bot definitions, routing, optional handlesOwnedSpaces, discordTokenEnvKey; lifecycleBotName in step bind; Arrietty template with workflowRef arrietty_room, create_thread step with storeIn deliveryChannelId, done step main-room redirect message.
- **Requirements:** REQ-CONFIG-001 — ConfigLoader parses YAML; workflow step bind keys (lifecycleBotName, deliveryChannelId); create_thread with storeIn (e.g. deliveryChannelId).
- **Assets:** ASSET-CONFIG-LOADER, ASSET-BOTS-YAML (config/bots.yaml), ASSET-BOT-CONFIG, ASSET-BOT-DEFINITION.
