# Compressed spec + mkdoc context (for update)

## Scope
Affected features: workflow/workflow-steps, bot/routing, config/config

## Per feature

### workflow/workflow-steps
- **Feature:** Workflow step DSL and branching actions, active, doc_path: features/domain/workflow/workflow-steps.md, summary: WorkflowDefinition and step/action primitives define the configurable workflow DSL and branch execution rules.
- **Requirements:** REQ-WORKFLOW-001: Workflow state machine and config-driven runners — Workflow interface, WorkflowRunner, ConfigurableWorkflowRunner; session keys; prompt_for_field, capture_field (trimAndLower, transforms); branch (when key/value, operators equals, not_equals, blank, nonblank, contains, starts_with, regex, one_of; transform on state value); extract_event (fromEvent: from, storeIn, default, transforms); WorkflowTransforms (trim, lower, upper, default); StepResult clearKeys; create_thread action.
- **Assets:** ASSET-CONFIGURABLE-WORKFLOW-RUNNER (ConfigurableWorkflowRunner.java), ASSET-BRANCH-STEP (BranchStep.java), ASSET-CAPTURE-FIELD-STEP (CaptureFieldFromEventStep.java), ASSET-WORKFLOW-TRANSFORMS (WorkflowTransforms.java), ASSET-WORKFLOW-CONDITION-EVALUATOR (WorkflowConditionEvaluator.java), ASSET-EXTRACT-EVENT-FIELDS-STEP (ExtractEventFieldsStep.java), ASSET-STEP-RESULT, ASSET-WORKFLOW-DEFINITION, ASSET-PROMPT-FOR-FIELD-STEP, ASSET-CALL-ACTION-STEP, ASSET-DONE-STEP, etc.
- **Existing doc excerpts:** summary: present; how-it-works: present; change-log: present; contracts: present (WorkflowStep, transforms, branch operators, extract_event); tests: present.

### bot/routing
- **Feature:** Event routing and normalized context, active, doc_path: features/domain/bot/routing.md, summary: Router matches events to bots through RoutingRule and filters over normalized event view.
- **Requirements:** REQ-BOT-001: Route events to bots by routing rules and ownership — Router.match returns bot ids; LifecycleContextStore and handlesOwnedSpaces; single-owner precedence; RoutingRule (filter + botId); ordered policy list; Routing.java removed, RoutingRule.java is the rule model.
- **Assets:** ASSET-ROUTER (Router.java), ASSET-ROUTING (RoutingRule.java), ASSET-EVENT-FILTER, ASSET-ROUTING-FILTER, ASSET-NORMALIZED-EVENT-CONTEXT.
- **Existing doc excerpts:** summary: present; contracts: RoutingRule holds filter and botId; routing is ordered policy list.

### config/config
- **Feature:** Bot config from YAML, active, doc_path: features/domain/config/config.md.
- **Requirements:** REQ-CONFIG-001: ConfigLoader loads bots, routing, workflows; parses workflow block, routing filters (discordMention, discordAuthors), handlesOwnedSpaces, step bind keys.
- **Assets:** ASSET-CONFIG-LOADER (ConfigLoader.java), ASSET-BOT-CONFIG, ASSET-BOTS-YAML.
- **Existing doc excerpts:** summary: present.
