# Compressed spec + mkdoc context (for update)

## Scope
Affected features: connectors/discord, core/engine, config/config, workflow/cursor-gathering, bot/bot, tools/tools, core/core, workflow/workflow-steps

## Per feature

### connectors/discord
- **Feature:** Discord event source and reply, active, doc_path: features/domain/connectors/discord.md, summary: Discord event source and reply; JDA-backed gateway; OutboundDeliveryRouter for sender resolution; lifecycle sender resolution with no silent fallback.
- **Requirements:**
  - REQ-CONNECTORS-DISCORD-001: Discord event source and reply — DiscordEventSource implements EventSource; per-bot Discord identity via discordTokenEnvKey; Bootstrap registers one sender per bot when key set and token present. OutboundDeliveryRouter resolves sender from delivery target and lifecycle context (configuredBotId); default sender for non-lifecycle channels; for lifecycle channels uses configured bot's sender only, no silent fallback (logs error when that bot's sender unavailable). Criteria: DiscordEventSource exists and emits events; mention metadata and author in payload; per-bot identity and OutboundDeliveryRouter sender resolution; lifecycle channel uses configured bot sender only. Tests: UNIT-ENGINE-DISCORD-REPLY (VinekeepersEngineTest), UNIT-DISCORD-APP-REPLY-SINK (DiscordAppReplySinkTest), UNIT-DISCORD-EVENT-SOURCE, UNIT-DISCORD-EVENT-SOURCE-MENTIONS, UNIT-CREATE-CHANNEL-ACTION-DISCORD, UNIT-OUTBOUND-DELIVERY-ROUTER (OutboundDeliveryRouterTest), MANUAL-CONNECTORS-DISCORD.
- **Assets:**
  - ASSET-DISCORD-SOURCE: path=src/main/java/com/vinekeepers/connectors/DiscordEventSource.java, role=Discord event source and reply sender
  - ASSET-DISCORD-REPLY: path=src/main/java/com/vinekeepers/connectors/DiscordReplySender.java, role=Interface for sending replies
  - ASSET-DISCORD-GATEWAY: path=src/main/java/com/vinekeepers/connectors/JdaDiscordGateway.java, role=JDA-backed Discord gateway
  - ASSET-DISCORD-REPLY-SINK: path=src/main/java/com/vinekeepers/connectors/DiscordAppReplySink.java, role=Implements AppReplySink; uses OutboundDeliveryRouter for sender resolution
  - ASSET-OUTBOUND-DELIVERY-ROUTER: path=src/main/java/com/vinekeepers/connectors/OutboundDeliveryRouter.java, role=Routes outbound delivery; resolves sender from target and lifecycle context (configuredBotId); no silent fallback for lifecycle channels
- **Existing doc excerpts:** (summary, how-it-works, contracts, change-log updated in this run)

### core/engine
- **Feature:** Event-driven engine orchestration. REQ-CORE-003: Engine delivers replies via sink; for Discord, OutboundDeliveryRouter resolves sender by target and lifecycle configuredBotId; no silent fallback for lifecycle rooms. Assets: ASSET-ENGINE, ASSET-APP-REPLY-SINK, ASSET-OUTBOUND-RESPONSE, ASSET-REPLY-TARGET.

### config/config
- **Feature:** Bot config from YAML. ConfigLoader parses discordTokenEnvKey for per-bot Discord identity; lifecycleBotName in step bind. Assets: ASSET-CONFIG-LOADER, ASSET-BOTS-YAML, ASSET-BOT-DEFINITION.

### workflow/cursor-gathering
- **Feature:** Cursor-backed gathering; lifecycle room Phase 1; LifecycleContext has configuredBotId for sender resolution. Assets: ASSET-LIFECYCLE-CONTEXT, ASSET-LIFECYCLE-CONTEXT-STORE, ASSET-POST-CHANNEL-MESSAGE-ACTION, ASSET-LAUNCH-CURSOR-RUN-ACTION, ASSET-CREATE-CHANNEL-ACTION.

### bot/bot
- **Feature:** Bot definition; BotDefinition includes optional discordTokenEnvKey for per-bot Discord identity. Assets: ASSET-BOT-DEFINITION.
