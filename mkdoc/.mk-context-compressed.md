# Compressed spec + mkdoc context (for update)

## Scope
Affected features: core/core, config/config, workflow/cursor-gathering, bot/routing (plan_change: specs/core-registry.yml, config/bots.yaml, Router, RouterTest; implement: none, verification only).

## Per feature

### core/core
- **Feature:** Specs governance and bootstrap, active, doc_path: features/domain/core/core.md, summary: Spec metadata anchors traceability; VinekeepersApp plus Bootstrap wire the runtime.
- **Requirements:** REQ-CORE-001 (spec index and registry), REQ-CORE-002 (Bootstrap wires engine, config, connectors; pass LifecycleContextStore and handlesOwnedSpaces map to Router).
- **Assets:** ASSET-SPEC-INDEX (specs/specs.yml), ASSET-REGISTRY (specs/core-registry.yml), ASSET-APP, ASSET-BOOTSTRAP, ASSET-PACKAGE-MARKER.

### config/config
- **Feature:** Bot config from YAML, active, doc_path: features/domain/config/config.md. ConfigLoader loads bot definitions, routing, optional handlesOwnedSpaces, discordTokenEnvKey; lifecycleBotName in step bind; branch value "" for blank/empty in bots.yaml.
- **Requirements:** REQ-CONFIG-001 — ConfigLoader parses YAML; discordTokenEnvKey, handlesOwnedSpaces, discordAuthors, lifecycleBotName; branch "" for blank.
- **Assets:** ASSET-CONFIG-LOADER, ASSET-BOTS-YAML (config/bots.yaml), ASSET-BOT-CONFIG, ASSET-BOT-DEFINITION.

### workflow/cursor-gathering
- **Feature:** Cursor-backed gathering workflow, active, doc_path: features/domain/workflow/cursor-gathering.md. Luna/Arrietty; lifecycle room; branch value "" for blank in step bind (config/bots.yaml).
- **Requirements:** REQ-LUNA-001 (lifecycle room, create_channel, provision_bot_instance, launch_cursor_run; confirmation branch clear; branch "" for blank).
- **Assets:** ASSET-BOTS-YAML, ASSET-LIFECYCLE-CONTEXT, ASSET-LIFECYCLE-CONTEXT-STORE, ASSET-CREATE-CHANNEL-ACTION, ASSET-POST-CHANNEL-MESSAGE-ACTION, ASSET-LAUNCH-CURSOR-RUN-ACTION, etc.

### bot/routing
- **Feature:** Event routing and normalized context, active, doc_path: features/domain/bot/routing.md. Router matches events by routing rules and ownership; when lifecycle owner lacks handlesOwnedSpaces, Router logs a warning and uses filter-based routing.
- **Requirements:** REQ-BOT-001 — Router.match returns bot ids; LifecycleContextStore + handlesOwnedSpaces; single-owner precedence when owner has handlesOwnedSpaces; when owner does not have handlesOwnedSpaces, log warning and use filter-based routing.
- **Assets:** ASSET-ROUTER (Router.java), ASSET-ROUTING, ASSET-EVENT-FILTER, ASSET-ROUTING-FILTER, ASSET-NORMALIZED-EVENT-CONTEXT.
- **Tests:** RouterTest (routeWithLifecycleStore_ownedChannel_logsOwnershipMismatchWarningWhenOwnerDoesNotHaveHandlesOwnedSpaces, etc.).
