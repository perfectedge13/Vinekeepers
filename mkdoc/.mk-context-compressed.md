# Compressed spec + mkdoc context (for update)

## Scope
Affected features: config/config, bot/routing, bot/bot, core/core, core/engine, connectors/discord

## Per feature

### config/config
- **Feature:** Bot config from YAML, active, doc_path: features/domain/config/config.md, summary: ConfigLoader loads bot definitions from YAML; dual-read identities.discord and legacy keys; normalize to ConnectorIdentity.
- **Requirements:** REQ-CONFIG-001: Load bot config from YAML — ConfigLoader loads bot definitions, routing, workflows; dual-read identities.discord (tokenEnvKey, handlesOwnedSpaces) and legacy top-level; normalize into ConnectorIdentity per connector; defaultDiscordTokenEnvKey retained transitional.
- **Assets:** ASSET-CONFIG-LOADER: path=ConfigLoader.java, role: Load bot definitions, routing; dual-read identities.discord and legacy; normalize to ConnectorIdentity. ASSET-BOTS-YAML: config/bots.yaml. ASSET-BOT-CONFIG: BotConfig.java. ASSET-BOT-DEFINITION: BotDefinition.java (getConnectorIdentity).
- **Existing doc excerpts:** summary, how-it-works, change-log, contracts, tests — present; update change-log and contracts for identities.discord and ConnectorIdentity.

### connectors/discord
- **Feature:** Discord event source and reply, active, doc_path: features/domain/connectors/discord.md.
- **Requirements:** REQ-CONNECTORS-DISCORD-001: Per-bot identity via BotDefinition.getConnectorIdentity("discord"); ConnectorContext generic; DiscordConnectorConfig; ConnectorAdapter registerBots; ConnectorRegistry; handlesOwnedSpaces connector-scoped.
- **Assets:** ASSET-CONNECTOR-CONTEXT, ASSET-CONNECTOR-ADAPTER, ASSET-CONNECTOR-REGISTRY, ASSET-DISCORD-CONNECTOR-CONFIG, ASSET-DISCORD-CONNECTOR-ADAPTER, DiscordEventSource, OutboundDeliveryRouter, DiscordAppReplySink.
- **Existing doc excerpts:** present; add change-log entry for ConnectorRegistry/Adapter/Context and DiscordConnectorConfig/Adapter.

### bot/routing
- **Feature:** Event routing and normalized context, active. Router uses handlesMap from bot.getConnectorIdentity("discord").getAttribute("handlesOwnedSpaces").
- **Assets:** ASSET-ROUTER, ASSET-ROUTING-FILTER, ASSET-NORMALIZED-EVENT-CONTEXT.

### bot/bot
- **Feature:** Bot runtime definition and tool policy, active. BotDefinition holds connectorIdentities; getConnectorIdentity(connectorId); ConnectorIdentity getAttribute/getAttributes.
- **Assets:** ASSET-CONNECTOR-IDENTITY, ASSET-BOT-DEFINITION.

### core/core
- **Feature:** Specs governance and bootstrap, active. Bootstrap wires ConnectorRegistry, DiscordConnectorAdapter, adapter.registerBots(bots, ConnectorContext); build handlesMap from getConnectorIdentity("discord").
- **Assets:** ASSET-BOOTSTRAP, ASSET-APP.

### core/engine
- **Feature:** Event-driven engine orchestration, active. Reply delivery via connector sink registry; no change to doc for this handoff.
