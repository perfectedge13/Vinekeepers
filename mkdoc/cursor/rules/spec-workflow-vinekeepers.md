# Spec-workflow-vinekeepers

## Summary

Project-specific defaults live in `.cursor/project.yml` and `specs/specs.yml` (validation block). Optional; remove or rename when porting to another repo.

## Key points

- Rely on that repo's project.yml and specs when porting.
- No additional obligations beyond the core and main spec-workflow rules.


