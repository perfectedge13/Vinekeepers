{
  "name": "vinekeepers-specs",
  "version": "1.0.0",
  "description": "Spec validation and drift checks for Vinekeepers (npm scripts for nova-code gates)",
  "private": true,
  "scripts": {
    "validate-specs": "node scripts/validate-specs.cjs",
    "validate-drift": "node scripts/validate-drift.cjs",
    "validate-docs": "node scripts/validate-docs.cjs",
    "test:spec-tools": "node --test scripts/tests/*.test.cjs"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
