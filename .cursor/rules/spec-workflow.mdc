---
description: Spec-driven change workflow (specs.yml is source of truth). Core workflow + project defaults.
alwaysApply: true
---

Reference these first:
- @specs/specs.yml
- @.cursor/rules/spec-workflow-core.mdc

# Spec-driven workflow

You MUST follow the **core** spec workflow for all coding tasks: Schema Gate, Spec Drift Gate, workflow steps 1–9, removal/rename protocol, and final output format. See **spec-workflow-core.mdc** for the full rules. Validation and project defaults are defined in **spec-workflow-core.mdc**.
