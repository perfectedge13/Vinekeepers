---
description: This project's defaults are in .cursor/project.yml and specs/specs.yml (validation). Optional; delete or rename when porting.
---

# Project defaults (optional)

This project's defaults are in **.cursor/project.yml** and **specs/specs.yml** (validation block). This rule file is optional; when porting skills to another repo, delete or rename it and rely on that repo's project.yml and specs.
